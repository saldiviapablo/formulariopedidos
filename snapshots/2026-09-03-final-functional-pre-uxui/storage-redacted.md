# Storage

- WeWeb Storage está habilitado.
- Los adjuntos del solicitante se guardan únicamente en Storage Private.
- Cada registro `archivos` se vincula a un único servicio y se accede internamente mediante URL temporal firmada.
- Las rutas, nombres de archivos, tokens, metadatos concretos y objetos privados se omiten deliberadamente.
